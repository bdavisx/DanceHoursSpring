<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<configuration>
    <!-- Configure the database connection here -->
    <jdbc>
        <driver>org.postgresql.Driver</driver>
        <url>jdbc:postgresql://localhost:5432/dance_hours</url>
        <user>dance</user>
        <password>vvGd@PLio1P3</password>
    </jdbc>

    <generator>
        <database>
            <name>org.jooq.util.postgres.PostgresDatabase</name>

            <!-- All elements that are generated from your schema (A Java regular expression.
                 Use the pipe to separate several expressions) Watch out for
                 case-sensitivity. Depending on your database, this might be
                 important!

                 You can create case-insensitive regular expressions using this syntax: (?i:expr)

                 Whitespace is ignored and comments are possible.
                 -->
            <includes>.*</includes>

            <inputSchema>public</inputSchema>
        </database>

        <generate>
            <!-- Generation flags: See advanced configuration properties -->
        </generate>

        <target>
            <packageName>com.tartner.dancehours.querymodel.database</packageName>

            <!-- The destination directory of your generated classes -->
            <directory>D:/source/DanceHoursSpring/src/main/java</directory>
        </target>
    </generator>
</configuration>
